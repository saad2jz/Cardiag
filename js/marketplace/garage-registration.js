document.getElementById('garageRegistration')?.addEventListener('submit', async (event) => { event.preventDefault(); const form = event.currentTarget; const status = document.getElementById('garageRegistrationStatus'); const button = form.querySelector('button'); button.disabled = true; status.textContent = 'Envoi…'; try { const payload = Object.fromEntries(new FormData(form)); const response = await fetch('/api/garages/applications',{ method:'POST', headers:{'content-type':'application/json'}, body:JSON.stringify(payload) }); const result = await response.json(); if (!response.ok) throw new Error(result.error); form.reset(); status.textContent = 'Merci. Votre candidature est enregistrée et sera vérifiée avant publication.'; } catch (error) { status.textContent = error.message || 'Envoi impossible.'; } finally { button.disabled = false; } });
